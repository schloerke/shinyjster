import * as selectize from "./selectize";
import * as assert from "./assert";
import * as shiny from "./shiny";

const methods = {
  assert: assert,
  selectize: selectize,
  shiny: shiny,
};

export { methods };
